These PEM files are DEVELOPMENT keys so local tests and docker-compose can boot.

Generate new keys before any real deployment and replace these files
(or mount them as secrets). Do not reuse the repo keys in production.
